- var publicPath = webpack.publicPath
- var hash = webpack.hash
doctype html
html
  head
    title 
    script(src=publicPath+"/home.js?"+hash)
  body
    h1 My Site
    p this is a sample project.
    a(href="./any/path") try it
    pre
      |server state:
      |  publicPath: #{webpack.publicPath}
      |  hash: #{webpack.hash}
    pre#entry-state